apiVersion: 1

providers:
  - name: portail
    orgId: 1
    folder: Portail
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
